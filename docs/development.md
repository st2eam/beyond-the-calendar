# Development

```bash
npm install
npm run dev
npm run build
```

The first run starts with an empty local snapshot. Enter a birth date, then use the map, day point, chapters, and important days. The app migrates the previous draft's localStorage data into IndexedDB when available.
