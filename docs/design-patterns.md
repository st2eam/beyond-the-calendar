# Design patterns

- Use the day point as the smallest meaningful unit.
- Prefer native form controls for dates, files, checkboxes, and focus behavior.
- Keep state changes explicit and persist them through the IndexedDB snapshot.
- Use Canvas for the full life map and a compact semantic list for accessible navigation.
- Give user actions a short, visible confirmation without interrupting the writing flow.
